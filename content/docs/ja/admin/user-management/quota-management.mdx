---
title: "クォータ管理"
description: "ユーザーとグループに日次/週次/月次/累計の 4 次元で API 使用制限を設定し、AI サービスコストの管理とリソース過剰消費の防止を実現します。"
---

import { Callout } from 'fumadocs-ui/components/callout'

## クォータ階層

Cherry Studio は 2 段階のクォータ体系を採用しています：

1. **グループレベルクォータ**：グループ設定で構成し、全グループメンバーのデフォルト制限として機能。
2. **ユーザーレベルクォータ**：個別ユーザーのカスタムクォータ。グループデフォルトより優先。空欄の場合はグループデフォルトを継承。

## ユーザークォータの設定

1. **ユーザー管理**ページに移動。
2. 対象ユーザーの操作列で**クォータ**ボタンをクリック。

<img src="/assets/images/ja/admin/user-management-quota-dialog_ja.webp" alt="ユーザークォータ設定" />

3. 各次元の制限値を設定：

<table><thead><tr><th width="100">次元</th><th>説明</th></tr></thead><tbody><tr><td><strong>日次</strong></td><td>過去 24 時間の最大使用量。毎日自動リセット。</td></tr><tr><td><strong>週次</strong></td><td>過去 7 日間の最大使用量。毎週自動リセット。</td></tr><tr><td><strong>月次</strong></td><td>過去 30 日間の最大使用量。毎月自動リセット。</td></tr><tr><td><strong>累計</strong></td><td>累積総使用量の上限。自動リセットなし、手動リセットが必要。</td></tr></tbody></table>

<Callout type="info">
**ヒント：** 空欄のままにするとグループデフォルトを継承します。グループにも制限がない場合は無制限となります。
</Callout>

## 使用量の確認とリセット

各クォータ次元の横にある展開ボタンをクリックすると、使用量の詳細を確認できます：

<img src="/assets/images/ja/admin/user-management-quota-detail_ja.webp" alt="クォータ使用量詳細" />

* **当期使用量**：現在の期間で消費された量。
* **制限値**：設定された上限（「無制限」は未設定を意味）。
* **次回リセット**：クォータが自動リセットされる時刻。
* **使用量リセット**ボタン：当期の使用量を手動でゼロにリセット。

4. 完了したら**保存**をクリック。

<Callout type="warn">
**注意：** ユーザーの使用量がクォータに達すると、クォータがリセットされるか管理者が調整するまで、以降の API 呼び出しは拒否されます。
</Callout>
