---
title: "服务商管理 (阿里云百炼/Qwen)"
description: 此章节基于 Cherry Studio 企业版最新的模型列表截图编写。
---

import { Callout } from 'fumadocs-ui/components/callout'

# 3. 服务商管理 (阿里云百炼/Qwen)

### 概述

**路径：** 管理后台侧边栏 > **服务商管理**

Cherry Studio 企业版默认集成了 **阿里云百炼 (Aliyun Bailian)** 作为核心模型引擎。系统预置了标准的 OpenAI 兼容协议配置，企业无需繁琐的参数调试，仅需填入 API Key 即可一键解锁通义千问（Qwen）全系列模型及百炼托管的高性能开源模型。

<Callout type="info">
**默认策略与扩展性**

* **默认状态**：系统初始状态下，仅显示"阿里云百炼"这一个服务商。
* **扩展能力**：支持管理员手动添加其他服务商（如 本地 Ollama），实现多渠道模型聚合。
</Callout>

***

### 1. 激活默认服务商

系统列表中已存在 **"阿里云百炼"** 卡片。

<img src="/assets/images/zh/admin/provider-management-aliyun-bailian-qwen-1_zh.webp" alt="" />
1. 点击卡片右侧的 **⚙️ 设置** 按钮。
2. **API Key**：填入从阿里云百炼控制台获取的密钥（以 `sk-` 开头）。
3. **API Host**：保持系统默认值。
   * _地址_：`https://dashscope.aliyuncs.com/compatible-mode/v1`
4. 点击 **检查连接**，确保提示"API Key 有效"。
5. 点击 **保存**。

***

### 2. 模型体系详解

根据百炼最新的 API 策略，Cherry Studio 将模型资源分为两大阵营。理解这两类模型的区别，有助于管理员为不同部门分配资源。

#### A. 通义千问 (商业/闭源版)

_位于模型列表顶部的核心区域，代表了阿里最强的模型能力。_

<table><thead><tr><th width="138.00390625">模型系列</th><th width="129.8046875">典型 ID (Snapshot)</th><th width="325.71875">适用场景</th><th>建议配置</th></tr></thead><tbody><tr><td><strong>旗舰版 (Max)</strong></td><td><code>qwen-max</code><br /><code>qwen-max-2025-01-25</code></td><td><strong>复杂任务主力</strong>。逻辑推理、长文写作、深度指令遵循。能力对标 GPT-4o。</td><td>✅ 推荐作为 CEO/高管的默认模型。</td></tr><tr><td><strong>均衡版 (Plus)</strong></td><td><code>qwen-plus</code><br /><code>qwen-plus-latest</code></td><td><strong>性价比首选</strong>。速度快于 Max，成本大幅降低，适合日常办公助手。</td><td>✅ 推荐作为全员通用的默认模型。</td></tr><tr><td><strong>长文本 (Long)</strong></td><td><code>qwen-long</code></td><td><strong>文档分析</strong>。支持超长上下文，适合上传整本 PDF 进行摘要或问答。</td><td>🔧 配合"知识库"功能使用。</td></tr><tr><td><strong>推理版 (QwQ)</strong></td><td><code>qwq-plus</code></td><td><strong>深度思考</strong>。专门针对数学、代码、复杂逻辑链优化的推理模型。</td><td>🧠 适合研发或数据分析部门。</td></tr></tbody></table>

#### B. 通义千问开源系列 (托管版)

_阿里云百炼精选并托管的开源模型，运行在企业级 GPU 集群上，无需本地显卡。_

<table><thead><tr><th width="124.9921875">模型系列</th><th width="207.57421875">典型 ID</th><th>特点</th></tr></thead><tbody><tr><td><strong>Qwen 2.5</strong></td><td><code>qwen2.5-72b-instruct</code><br /><code>qwen2.5-32b-instruct</code></td><td><strong>开源最强</strong>。72B 版本在多项基准测试中超越 GPT-4，且调用成本极低。</td></tr><tr><td><strong>Coder 代码</strong></td><td><code>qwen-coder-plus</code><br /><code>codeqwen1.5-7b-chat</code></td><td><strong>编程专用</strong>。经过海量代码数据微调，适合作为 IDE 插件或代码审查助手。</td></tr><tr><td><strong>Math 数学</strong></td><td><code>qwen-math-plus</code></td><td><strong>理科专用</strong>。增强了数学解题和公式推导能力。</td></tr><tr><td><strong>Qwen 2 / 1.5</strong></td><td><code>qwen2-72b-instruct</code><br /><code>qwen1.5-110b-chat</code></td><td><strong>历史版本</strong>。提供对旧版模型的兼容支持，适合需要特定版本对齐的业务。</td></tr></tbody></table>

<Callout type="warn">
**关于版本号 (Dates)** 您在列表中会看到如 `qwen-max-2024-09-19` 的日期后缀版本。

* **最佳实践**：除非业务代码强依赖某个特定日期的模型表现，否则建议直接使用不带日期的 ID（如 `qwen-max` 或 `qwen-plus`），系统会自动指向最新稳定版 (`latest`)。
</Callout>

***

### 3. 管理模型列表

<img src="/assets/images/zh/admin/provider-management-aliyun-bailian-qwen-2_zh.webp" alt="" />
百炼提供的模型列表非常长（如上图所示），为了避免员工选择困难，管理员应进行精简。

1. 点击百炼卡片下的 **𝌡 管理模型**。
2. **筛选策略**：
   * 保留 `qwen-max` 和 `qwen-plus` (不带日期的版本)。
   * 保留 `qwq-plus` (用于推理任务)。
   * 保留 `qwen2.5-72b-instruct` (高性价比大模型)。
   * 保留 `qwen-coder-plus` (研发专用)。
3. **隐藏冗余**：将所有带旧日期（如 `2024-04-28`）或过小参数（如 `0.5b`, `1.5b`）的模型点击 **-** 号隐藏。
4. 点击 **保存**。

***

### 4. 添加其他服务商 (自定义)

虽然百炼是默认供应商，但 Cherry Studio 作为一个聚合平台，完全支持接入其他渠道。

**场景举例：**

* 您想使用 **OpenAI** 原生的 `gpt-4o`。
* 您在本地服务器部署了 **Ollama**。

**操作步骤：**

1. 在服务商列表底部，点击 **+ 添加服务商**。
2. **服务商类型**：
   * 选择 **OpenAI** (通用兼容型)。
   * 选择 **Anthropic** (Claude 系列)。
   * 选择 **Ollama** (本地模型)。
3. 填写对应的 **API Key** 和 **API Host**。
   * _例如本地 Ollama Host_: `http://192.168.1.5:11434/v1`
4. 添加后，该服务商的模型将与百炼模型共同出现在用户的模型选择下拉框中。

***

### 5. 常见问题 (FAQ)

**Q: 列表中 `qwq-32b-preview` 和 `qwq-plus` 有什么区别？**&#x20;

A: `qwq-32b` 是基于开源权重的推理模型，而 `qwq-plus` 是阿里云自研的商业闭源推理模型，通常后者在逻辑上限和安全性上更高，但成本也相对较高。

**Q: 这么多版本，我该选 `qwen-max` 还是 `qwen-max-latest`？**&#x20;

A: 在阿里云百炼中，`qwen-max` 通常是指向当前稳定版本的别名，而 `qwen-max-latest` 可能包含最新的实验性优化。在生产环境中，直接选用 `qwen-max` 最为稳妥。
