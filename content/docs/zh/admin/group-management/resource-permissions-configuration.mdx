---
title: "资源权限配置"
description: 这是分组管理中最关键的环节。通过配置 Assistant（助手） 和 Model（模型） 的白名单，您可以精确定义该组成员的 AI 工作环境。
---

import { Callout } from 'fumadocs-ui/components/callout'

# 2. 资源权限配置

### 1. 配置助手权限 (Assistants)

控制该分组用户在 Cherry Studio 客户端左侧“助手”栏中可见的预设智能体。

* **全部助手**：勾选此项，该组用户可访问企业库中的所有公共助手。
* **指定助手**：**（推荐）** 仅勾选该业务线需要的助手，保持界面简洁。

**操作步骤：**

1. 在创建/编辑窗口中，找到 **可使用的助手** 区域。
2. 选择 **指定助手**。
3. 在下拉列表中勾选目标助手（如 `翻译助手`, `代码审查员`）。

<img src="/assets/images/zh/admin/group-management-resource-permissions-1_zh.webp" alt="" />
### 2. 配置模型权限 (Models)

控制该分组用户在对话框顶部的模型下拉菜单中，可以选择哪些 LLM 进行对话。

<Callout type="warn">
**成本控制提示：** 合理配置模型权限是控制企业 API 支出的最有效手段。建议仅向核心研发或管理团队开放 GPT-4 等高价模型。
</Callout>

**配置步骤：**

1. 找到 **可使用的模型** 区域。
2. 选择 **指定模型**。
3. 系统将列出所有已配置的 **Model Provider（模型服务商）**。
4. 您可以进行以下操作：
   * **按服务商全选：** 勾选 `OpenAI` 或 `DeepSeek` 标题旁的复选框，一键授权该服务商下的所有模型。
   * **按模型单选：** 展开服务商列表，仅勾选特定的 Model ID（如 `gpt-5`, `deepseek-coder`）。
5. 利用搜索框快速查找模型名称。
6. 勾选 **仅显示已选** 复选框，二次确认分配的资源列表是否正确。

<img src="/assets/images/zh/admin/group-management-model-selector-ui_zh.webp" alt="模型选择器界面" />
### 3. 验证权限配置

为确保权限配置无误，建议进行以下“冒烟测试”：

1. 创建一个测试用户，将其分配至该新分组。
2. 使用测试账号登录 Cherry Studio 客户端。
3. **检查点 A：** 打开新对话，点击顶部的模型列表。确认列表 **仅包含** 您刚才勾选的模型。
4. **检查点 B：** 前往助手库，确认仅显示授权的助手。
