---
title: "用户管理"
description: 集中管理企业用户的账号、权限与 API 凭据。通过分组机制精确控制各部门可访问的 AI 模型与助手资源，兼顾安全合规与成本管控。
icon: Users
---

import { Callout } from 'fumadocs-ui/components/callout'

## 核心功能

在此模块中，您可以执行以下关键任务：

* **全生命周期管理**：支持从账号创建、信息变更（邮箱、密码、手机号）到账号注销/删除的完整流程。
* **精细化权限分配**：通过**分组 (Groups)** 机制，将用户与特定的模型资源池绑定，实现"研发组用 Claude，市场组用 GPT-4"的差异化配置。
* **安全阻断**：支持一键切换用户的**激活/未激活**状态，在员工离职或账号异常时，能够毫秒级阻断其登录与 API 调用权限。
* **API 网关凭证**：为开发者或自动化脚本生成独立的 **API Key**，使其无需通过客户端即可调用 Cherry Studio 集成的模型能力。
* **多维度审计**：提供直观的列表视图，支持通过用户名检索，并可按账号状态或分组进行筛选，快速定位目标用户。

## 使用场景

<table><thead><tr><th width="107.89453125">场景</th><th>操作指引</th></tr></thead><tbody><tr><td><strong>新员工入职</strong></td><td>创建新账号并分配至对应业务部门的分组。</td></tr><tr><td><strong>员工转岗</strong></td><td>编辑用户信息，修改其所属分组以变更模型使用权限。</td></tr><tr><td><strong>开发测试</strong></td><td>为开发人员生成专属的 API Key 以测试后端模型接口。</td></tr><tr><td><strong>账号冻结</strong></td><td>发现异常行为或员工离职时，立即关闭"激活状态"。</td></tr></tbody></table>

## 快速开始

请在管理后台左侧导航栏选择 **用户管理** 进入主界面。

<Callout type="warn">
**重要提示：** 用户创建后的**权限范围**完全取决于其所属的 **分组**。在添加用户之前，建议您先在"分组管理"模块中规划好企业的组织架构及对应的模型授权策略。
</Callout>
